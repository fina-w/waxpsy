# Forcer l'utilisation de npm pour les dépendances optionnelles
optional=false
# Désactiver le moteur de rendu natif de Tailwind CSS
tailwindcss.engine=classic
# Forcer la reconstruction des dépendances natives
build-from-source=true
# Désactiver le cache pour éviter les problèmes de dépendances
cache-max=0
prefer-offline=false